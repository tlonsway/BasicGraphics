#version 410 core

layout (location = 0) in vec3 aPos;

out vec4 ourColor;

uniform mat4 fullMat;
uniform mat4 model;

void main() {
	sunPos = vec3(1000.0f,1000.0f,1000.0f);
	gl_Position = vec4(aPos,1.0f);
	
	
	
	ourColor = vec4(aColor,0.6f);
}
